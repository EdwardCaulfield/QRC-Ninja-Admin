import { CallToActionDirective } from './call-to-action.directive';

describe('CallToActionDirective', () => {
  it('should create an instance', () => {
    const directive = new CallToActionDirective();
    expect(directive).toBeTruthy();
  });
});
